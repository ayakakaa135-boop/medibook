# MediBook (نظام حجز مواعيد العيادات) 🏥

**MediBook** هو نظام حديث ومتكامل لإدارة وحجز مواعيد العيادات الطبية، مبني باستخدام إطار العمل Django مع التركيز على الأداء العالي، واجهة المستخدم التفاعلية، ودعم اللغات المتعددة.

---

## ✨ المميزات الرئيسية

- 📅 **إدارة المواعيد**: نظام حجز متطور وسهل الاستخدام للمرضى والعيادات.
- 🌍 **دعم اللغات (i18n)**: واجهة كاملة باللغتين العربية والإنجليزية.
- 👨‍⚕️ **إدارة الأطباء والخدمات**: عرض شامل للأطباء وتخصصاتهم والخدمات المتاحة.
- ⚡ **واجهة تفاعلية**: استخدام **HTMX** لتجربة مستخدم سريعة وديناميكية بدون إعادة تحميل الصفحة.
- 📱 **تصميم متجاوب**: واجهة عصرية متوافقة مع كافة الشاشات.
- 🛡️ **نظام أمان متقدم**: إدارة المستخدمين والصلاحيات باستخدام `django-allauth`.
- ⚙️ **مهام خلفية**: إدارة المهام الثقيلة والتنبيهات المجدولة باستخدام **Celery** و **Redis**.
- 🐳 **دعم Docker**: سهولة النشر والتشغيل باستخدام الحاويات.

---

## 🛠️ التقنيات المستخدمة (Tech Stack)

- **Backend**: Django 5.1
- **Frontend**: HTML5, CSS3, JavaScript (HTMX, Alpine.js)
- **Database**: PostgreSQL (Supabase)
- **Caching & Backend Tasks**: Redis & Celery
- **Styles**: Tailwind CSS / SCSS (django-compressor + libsass)
- **Authentication**: Django Allauth
- **Static Files**: WhiteNoise

---

## 🚀 التشغيل والإعداد

### المتطلبات الأساسية
- Python 3.11+
- PostgreSQL
- Redis (اختياري للمهام الخلفية)

### التثبيت المحلي

1. **نسخ المستودع:**
   ```bash
   git clone <repository-url>
   cd medibook
   ```

2. **إنشاء بيئة افتراضية وتثبيت الاعتمادات:**
   ```bash
   python -m venv .venv
   source .venv/bin/activate  # لنظام Linux/Mac
   .venv\Scripts\activate     # لنظام Windows
   pip install -r requirements.txt  # أو استخدام uv sync إذا كنت تستخدم uv
   ```

3. **إعداد المتغيرات البيئية:**
   قم بإنشاء ملف `.env` بناءً على `.env.example` وأضف بيانات قاعدة البيانات والمفاتيح السرية.

4. **تشغيل التهجيرات (Migrations):**
   ```bash
   python manage.py migrate
   ```

5. **تشغيل الخادم:**
   ```bash
   python manage.py runserver
   ```

### التشغيل باستخدام Docker

يمكنك بدء تشغيل المشروع بالكامل (Django, Postgres, Redis, Celery) بضغطة زر واحدة:

```bash
docker-compose up --build
```

---

## 📂 هيكلية المشروع

- `apps/`: تحتوي على تطبيقات المشروع (المواعيد، العيادات، المستخدمين، إلخ).
- `config/`: إعدادات المشروع الأساسية وربط الـ URLs.
- `templates/`: ملفات القوالب الخاصة بـ Django.
- `static/` & `media/`: الملفات الثابتة والوسائط.
- `locale/`: ملفات الترجمة (العربية/الإنجليزية).

---

## 🤝 المساهمة
إذا كنت ترغب في المساهمة في تطوير MediBook، يسعدنا استقبال مقترحاتك عبر الـ Pull Requests.

---

## 📄 الترخيص
هذا المشروع متاح تحت رخصة MIT.
